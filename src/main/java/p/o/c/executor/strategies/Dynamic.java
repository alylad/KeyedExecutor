package p.o.c.executor.strategies;

/**
 * A class implementing this marker interface can dynamically adjust to load, meaning a key/hash can
 * move across executing Threads during the lifetime of the JVM
 */
public interface Dynamic {
}
